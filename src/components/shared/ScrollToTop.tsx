/**
 * In Astro's MPA mode every page navigation is a full browser reload,
 * so the browser already scrolls to the top automatically.
 * This component is kept as a no-op for compatibility.
 */
export const ScrollToTop = () => null;
